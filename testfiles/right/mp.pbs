#!/bin/sh
#PBS -N Al2O2
#PBS -S /bin/bash
#PBS -j oe
#PBS -o opt.inp.out
#PBS -W umask=022
#PBS -l walltime=5000:00:00
#PBS -l ncpus=8
#PBS -l mem=64gb

module load intel
module load mvapich2
module load pbspro
export PATH=/usr/local/apps/molpro/2015.1.35/bin:$PATH

export WORKDIR=$PBS_O_WORKDIR
export TMPDIR=/tmp/$USER/$PBS_JOBID
cd $WORKDIR
mkdir -p $TMPDIR

date
parallel -j 8 < opt.inp
date

rm -rf $TMPDIR
